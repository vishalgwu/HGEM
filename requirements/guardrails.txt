# ---------------------------------------------------------------------------
# GuardMem AI - security armor
# ---------------------------------------------------------------------------
# RULES.md 4 names memory poisoning and indirect prompt injection as the two
# primary threats. Guardrails run pre-flight, on RAW text, before any model sees
# it (ARCHITECTURE.md 2, box 2).
#
# Injection detection (S11.1) is deliberately dependency-free: heuristic
# patterns + a FAST-tier classifier + the canary check from S2.2. It needs no
# library. PII detection does.
#
# Introduced: BUILD_NOTEBOOK.md S11.3
# ---------------------------------------------------------------------------

presidio-analyzer==2.2.362    # S11.3 PII detection + custom MRN/case-number recognizers
presidio-anonymizer==2.2.362  # S11.3 reversible tokenization -> <PII:PERSON:tok_9a1f>
spacy==3.8.16                 # presidio's NLP engine

# REQUIRED EXTRA STEP - spacy models are not PyPI dependencies and so cannot
# appear in requirements.lock.txt. After installing this file, run:
#
#     python -m spacy download en_core_web_lg      # resolves to en_core_web_lg 3.8.0, ~400 MB
#
# Without it presidio raises at analyzer construction, not at import, so the
# failure shows up at runtime rather than at install time. Already installed in
# the HGEM venv and verified: AnalyzerEngine() tags PERSON and PHONE_NUMBER on a
# sample clinical string. It does NOT tag MRN - that is exactly why S11.3 calls
# for custom recognizers for MRN and case numbers.
#
# The policy engine (S12.2) is "Rego-compatible" (PRD FR-6.4). OPA is a Go
# binary, not a pip package - install it separately if you choose to shell out
# to it rather than evaluating packs in-process.
